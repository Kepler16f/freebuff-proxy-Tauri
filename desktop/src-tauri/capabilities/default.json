{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "主窗口核心权限：允许 splash 使用事件与窗口 API（core:default 含 event/window/webview 默认集）",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
