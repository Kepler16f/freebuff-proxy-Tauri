node_modules/
dashboard/node_modules/
dashboard/dist/
# 构建产物：版本号由发版流水线注入（scripts/inject-version.mjs），不入库
dashboard/version.json

# 本地运行数据（Docker 挂载 /data 到宿主机时同样忽略）
data/
*.log
*.pid

# 上游账号凭据（credentials JSON 视为机密，勿提交）
credentials/*.json
credentials/active
# OpenAPI 账号导入文件同样含 authToken，禁止入库
accounts-import.json

# 环境配置（本地运行配置，不入库；Docker 场景固定读 /data/config.yaml）
config.toml
config.yaml
# 本地开发用的 compose 覆盖（源码热挂载等），只属于本机
# 注意：不要用 docker-compose.override.yml —— Docker Compose 会自动加载它，
# 那份覆盖只在本机存在，别的机器 git clone 后 compose 行为会不一致；
# 需要叠加时就手动 docker compose -f docker-compose.yml -f xx.local.yml
*.local.yml
.env
.env.*
!.env.example
config.json
config.local.json

# 输出文件
accounts.csv
nvidia_api_key.txt
captcha_artifacts/
failure_artifacts/
Results/

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
/build/

# 系统 / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
.ruff_cache/

# 本地 e2e 测试实例数据（fb-test 容器挂载）
data-test/

# Tauri 桌面客户端（desktop/）的本地构建产物
desktop/node_modules/
desktop/src-tauri/target/
desktop/src-tauri/resources/
